{
  "description": "E2E concurrency test: 3 clients submit to the single-GPU service; server processes one at a time (FIFO); client3 polls and pulls its artifacts. Each client first registers (POST /v1/clients {client_id}) to get a secret client_key, sent as the X-Client-Key header to enqueue and to list its own jobs.",
  "base_url": "http://localhost:8091",
  "register": {"method": "POST", "path": "/v1/clients", "content_type": "application/json",
               "body": {"client_id": "<client1|client2|client3>"},
               "returns": "{client_id, client_key}  (client_key shown once; send as 'X-Client-Key' on every enqueue/list)"},
  "clients": {
    "client1_youtube": [
      {"method": "POST", "path": "/v1/tasks", "content_type": "application/json",
       "body": {"source": "https://youtu.be/3Amlu4y94Ho", "client_id": "client1", "clip": "0-20", "name": "c1_youtube"}}
    ],
    "client2_bilibili": [
      {"method": "POST", "path": "/v1/tasks", "content_type": "application/json",
       "body": {"source": "https://b23.tv/fZQNYqJ", "client_id": "client2", "clip": "0-60", "name": "c2_bilibili"},
       "note": "Bilibili (b23.tv short link). Works because the worker uses the logged-in cookies from scripts/bili_login.py (data/bili_cookies.txt / $YT_COOKIES); without them a datacenter IP gets HTTP 412."}
    ],
    "client3_file_and_multiple": [
      {"method": "POST", "path": "/v1/tasks/upload", "content_type": "multipart/form-data",
       "file": "benchmark/sample/allin_60s_16k.wav", "form": {"client_id": "client3", "speakers": "2", "name": "c3_file"}},
      {"method": "POST", "path": "/v1/tasks", "content_type": "application/json",
       "body": {"source": "https://youtu.be/3Amlu4y94Ho", "client_id": "client3", "clip": "40-55", "name": "c3_req2"}},
      {"method": "POST", "path": "/v1/tasks", "content_type": "application/json",
       "body": {"source": "https://youtu.be/3Amlu4y94Ho", "client_id": "client3", "clip": "60-75", "name": "c3_req3"}}
    ]
  },
  "note_auth": "Every POST /v1/tasks and /v1/tasks/upload carries the header 'X-Client-Key: <client_key>' from registration; without it the enqueue is 401. A client lists its own jobs with GET /v1/tasks + X-Client-Key (sees ONLY its tasks).",
  "then": "Each POST returns a per-task pull_token. client3 polls GET /v1/tasks/{id} with header 'X-Task-Token: <pull_token>' (or its X-Client-Key) until done, then GET /v1/tasks/{id}/artifact (same auth) -> zip. /v1/queue is the admin view (bearer-gated when TTS_SERVE_API_KEY set)."
}
